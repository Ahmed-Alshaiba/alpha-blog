<div class="container">
  <div class="jumbotron p-3 shadow mb-5 bg-gray rounded">
    <%= form_with(model: @user, url: login_path, local: true) do |f| %>
      <div class="form-group justify-content-center row">
        <%= f.label :email, class: "col-10 col-form-label mr-2" %>
        <div class="col-10">
          <%= f.email_field :email, class: "form-control shadow rounded", placeholder: "Enter your email" %>
        </div>
      </div>
      <div class="form-group justify-content-center row">
        <%= f.label :password, class: "col-10 col-form-label mr-2" %>
        <div class="col-10">
          <%= f.password_field :password, class: "form-control shadow rounded", placeholder: "Enter your password" %>
        </div>
      </div>
      <div class="text-center">
        <%= f.submit "Log-in", class: "btn btn-outline-success shadow rounded" %>
      </div>
    <% end %>
  </div>
</div>
<div class="text-center">
  <%= link_to 'Cancel and Return to the home page', root_path, class: "btn btn-info shadow rounded mb-3" %>
</div>