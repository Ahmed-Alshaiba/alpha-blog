<h1 class="page-name mt-4">Alpha bloggers</h1>

<div class="flickr_pagination">
  <%= will_paginate @articles, :container => false %>
</div>

<%= render 'users' %>

<div class="flickr_pagination mb-3">
  <%= will_paginate @users, :container => false %>
</div>